// An empty PostCSS configuration file to resolve build errors.
// This prevents Vite from looking for a local Tailwind CSS installation,
// as the project uses the Tailwind CDN script instead.
module.exports = {
  plugins: [],
};
